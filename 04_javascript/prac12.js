let no_1 = 1;
let no_2 = 2;
let no_3 = 3;

function add(a, b, c) {
    let numAdd = a + b + c;
    console.log(numAdd);
    alert(numAdd);
}
add(no_1, no_2, no_3);


function sub(x, y) {
    let numSub = x - y;
    console.log(numSub);
    alert(numSub);
}
sub(no_1, no_2);

function divide(x, y) {
    let numDiv = x / y;
    console.log(numDiv);
    alert(numDiv);
}
divide(no_1, no_2);


function mul(x, y) {
    let numMul = x * y;
    console.log(numMul);
    alert(numMul);
}
mul(no_1, no_2);

